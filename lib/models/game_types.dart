enum GameState {
  menu,
  playing,
  paused,
  won,
  lost,
}

enum EnemyType {
  scout,
  standard,
  heavy,
  boss,
}

// ============================================================
// HACKABLE: difficulty options
// ============================================================
enum DifficultyMode {
  easy,
  medium,
  hard,
}
